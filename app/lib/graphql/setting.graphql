query Setting($settingId: ObjectId!) {
  setting(settingId: $settingId) {
    _id
    sound
    music
    volume
    money
    rate
    balance
    games
    complexity
    user {
      _id
      username
      email
    }
  }
}
